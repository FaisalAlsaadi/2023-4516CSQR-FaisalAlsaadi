##PROGRAM GOALS
#1- Show full dataframe with sorting by name/population/yearly change/net change/density/land area/migrants/fertility rate/median age/urban population/world share/region
#2- Show global population -->  visualization with matplotlib for average yearly change + net change + overall population + average land area + fertility rate + urban population
#3- Show statistics for each continent with matplotlib visualization --> average net change + fertility + med age + urban population + worldshare + migrants + land area + overall population
#4- Search for specific country and show its statistics
##TO DO: -CORRECTLY NAME MENU OPTIONS
##       -LOAD PACKAGES AND DATAFRAME
##       -CORRECTLY LOAD THE CORRECT INFORMATION ON EACH OF THE MENU OPTIONS
##       -FIX SEARCH FUNCTION ONCE EVERYTHING ELSE IS DONE (OPTION 4 FROM MAIN MENU)
##       -MORE REFINED DOCUMENTATION
#import packages and load dataframe




#program welcome message and menu for user to choose operation
#MENU OPTIONS
while True:
    # Display main menu options
    print("Data on World Populations")
    print("4516CSQR Group Project for Faisal Al-Saadi, Nassir Al-Ansari, Abdullah Al-Dahimi")
    print("Menu:")
    print("1. Countries sorted by name")
    print("2. Analysis for global populations")
    print("3. Data on each continent")
    print("4. Search for a specific country")
    
    # Prompt user to enter a choice
    choice = input("Choose your option: ")
    
    # Check if input is valid
    if choice.isdigit() and int(choice) in range(1, 5):
        # Input is valid, execute corresponding option
        if choice == "1":
             # Display sub menu options for option 1
            print("Option 1 Sub Menu:")
            print("1. Sub Option 1")
            print("2. Sub Option 2")
            print("3. Sub Option 3")
            print("4. Sub Option 4")
            print("5. Sub Option 5")
            print("6. Sub Option 6")
            print("7. Sub Option 7")
            print("8. Sub Option 8")
            print("9. Sub Option 9")
            print("10. Sub Option 10")
            print("11. Sub Option 11")
            
            # Prompt user to enter a sub menu choice
            sub_choice = input("Enter a number from 1 to 11: ")
             # Check if sub menu input is valid
            if sub_choice.isdigit() and int(sub_choice) in range(1, 12):
                # Sub menu input is valid, execute corresponding sub option
                print("Option 1, Sub Option", sub_choice, "selected")
                break
            else:
                # Sub menu input is invalid, display error message
                print("Invalid input. Please enter a number from 1 to 11.")
            
        elif choice == "2":
            print("Option 2 selected")
        elif choice == "3":
            print("Option 3 selected")
             # Display sub menu options for option 3
            print("Option 3 Sub Menu:")
            print("1. Sub Option 1")
            print("2. Sub Option 2")
            print("3. Sub Option 3")
            print("4. Sub Option 4")
            print("5. Sub Option 5")
            print("6. Sub Option 6")
            
            # Prompt user to enter a sub menu choice
            sub_choice = input("Enter a number from 1 to 6: ")
            
            # Check if sub menu input is valid
            if sub_choice.isdigit() and int(sub_choice) in range(1, 7):
                # Sub menu input is valid, execute corresponding sub option
                print("Option 3, Sub Option", sub_choice, "selected")
                break
            else:
                # Sub menu input is invalid, display error message
                print("Invalid input. Please enter a number from 1 to 6.")
        elif choice == "4":
            print("Option 4 selected")
             # Prompt user to enter a search string
            search_term = input("Enter a search term: ")
            #fix this search to search dataframe countries names
        matches = [item for item in dataset if search_term in item]
        if len(matches) > 0:
            print(f"Found {len(matches)} match(es):")
            for match in matches:
                print(match)
        else:
            print("No matches found.")
            
            
        break
    else:
        # Input is invalid, display error message
        print("Invalid input. Please enter a number from 1 to 4.")
